@extends('layouts.admin') @section('title','Clientes') @section('eyebrow','Administración') @section('page-title','Clientes privados') @section('page-description','Solo los clientes creados aquí pueden entrar. No existe registro público.') @section('content') @include('platform._header')
<div class="grid xl:grid-cols-[.7fr_1.3fr] gap-6"><form method="POST" action="{{ route('clients.create') }}" class="bg-white border rounded-[24px] p-7 space-y-4">@csrf<h2 class="font-display font-bold text-xl">Registrar cliente</h2><input name="company" required placeholder="Empresa" class="w-full border rounded-xl px-4 py-3"><input name="name" required placeholder="Nombre del propietario" class="w-full border rounded-xl px-4 py-3"><input name="email" type="email" required placeholder="correo@empresa.com" class="w-full border rounded-xl px-4 py-3"><input name="password" type="password" required minlength="12" placeholder="Contraseña temporal (12+ caracteres)" class="w-full border rounded-xl px-4 py-3"><button class="bg-ink text-white rounded-xl px-5 py-3 font-semibold">Crear acceso privado</button></form><div class="space-y-3">@foreach($clients as $client)<div class="bg-white border rounded-2xl p-5 flex justify-between"><div><strong>{{ $client->name }}</strong><p class="text-sm text-gray-500">{{ $client->users->pluck('email')->join(', ') }}</p></div><span class="text-xs rounded-full px-2 py-1 h-fit bg-emerald-100 text-emerald-700">{{ $client->status }}</span></div>@endforeach</div></div>@endsection
