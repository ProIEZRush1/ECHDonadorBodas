@extends('layouts.admin') @section('title','Facturación') @section('eyebrow','Consumo y pagos') @section('page-title','Facturación de IA') @section('page-description','Agrega un método de pago para cubrir el consumo de conversaciones automatizadas.') @section('content') @include('platform._header')
<div class="grid lg:grid-cols-2 gap-6"><div class="rounded-[28px] bg-gradient-to-br from-violet to-indigo-800 text-white p-7 min-h-64 flex flex-col justify-between shadow-soft"><div class="flex justify-between"><span class="font-display font-bold">Buy Overcloud</span><span class="text-white/50">IA · WhatsApp</span></div>@if($billing?->status==='active')<div><p class="text-2xl tracking-[.2em]">•••• •••• •••• {{ $billing->card_last_four }}</p><div class="flex justify-between mt-5 text-sm"><span>{{ strtoupper($billing->card_brand) }}</span><span>{{ $billing->card_expiry }}</span></div></div>@else<div><p class="text-2xl font-display font-bold">Sin método de pago</p><p class="text-white/60 text-sm mt-2">Activa el consumo de IA agregando una tarjeta.</p></div>@endif</div><div class="bg-white rounded-[24px] border p-7"><h2 class="font-display text-xl font-bold">Método de pago</h2><p class="text-sm text-gray-500 mt-2">Serás redirigido al portal seguro de Stripe. Buy Overcloud solo recibe la marca y los últimos cuatro dígitos.</p><div class="bg-cloud rounded-xl p-4 mt-6 text-sm"><div class="flex justify-between"><span>Estado</span><strong class="{{ $billing?->status==='active'?'text-emerald-600':'text-amber-600' }}">{{ $billing?->status==='active'?'Activo':'Pendiente' }}</strong></div><div class="flex justify-between mt-3"><span>Moneda</span><strong>MXN</strong></div></div><form method="POST" action="{{ route('billing.setup') }}" class="mt-6">@csrf<button class="bg-ink text-white rounded-xl px-5 py-3 font-semibold">{{ $billing?->status==='active'?'Cambiar tarjeta':'Agregar tarjeta' }} →</button></form></div></div>@endsection
